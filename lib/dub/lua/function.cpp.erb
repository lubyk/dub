<%= comment(@function) %>
<%= signature(@function) %> {
  try {
<%= indent(body(@function), 4) %>
  } catch (std::exception &e) {
    return luaL_error(L, "<%= @function.id_name %>: %s", e.what());
  } catch (...) {
    return luaL_error(L, "<%= @function.id_name %>: Unknown exception");
  }
}